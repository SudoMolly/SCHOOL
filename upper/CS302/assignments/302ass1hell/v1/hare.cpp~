

int make_hare(vector<hare> & h,int max, int i)
{
    char * tempc;
    string temps;
    if(!case1_strings(temps,tempc)) exit(__LINE__);
    h.emplace(i,temps,tempc);
    ++i;
    ~strcpy2(tempc);
    if (i == max) return i;
    return (make_hare(h,max,i));
}
